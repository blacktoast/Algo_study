/**
 * @param {number[]} height
 * @return {number}
 */
var maxArea = function (height) {
  let [start, end] = [0, height.length - 1];
  let max = -1;
  while (start <= end) {}
  console.log(height);
};

maxArea([1, 8, 6, 2, 5, 4, 8, 3, 7]);
